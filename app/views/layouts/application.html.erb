<!DOCTYPE html>
<html>
  <head>
    <title>NewRailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= stylesheet_link_tag 'application', 'data-turbolinks-track': 'reload', 'integrity': '...', 'crossorigin': 'anonymous' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload', 'integrity': '...', 'crossorigin': 'anonymous' %>
    <%= csrf_meta_tags %>
  </head>

  <body style="padding-top: 20px;"> <!-- Adjust the padding value as needed -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <%= link_to 'Your Movie Lists', root_path, class: 'navbar-brand' %>

        <% if @current_list %>
          <span class="navbar-text">
            <%= @current_list.name %> List
          </span>
        <% end %>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to 'Add List', new_list_path, class: 'nav-link' %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>
